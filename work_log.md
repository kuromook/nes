# 開発ログ (NES game)

ファミコン(NES)ゲーム開発の進捗メモ。ca65アセンブリ / NROM-256 (PRG 32KB + CHR 8KB, mapper 0) / エミュレータ FCEUX。

> ハードウェア不安定問題（このマシン固有・開発とは無関係）の調査記録は別ファイル `hardware_issue.md`（gitignore済）にある。

---

## 進捗

### ✅ 1. 開発環境の整備
- ツールチェーン: cc65 V2.18 (ca65 / ld65)、make、git（導入済み）
- エミュレータ: FCEUX (`/usr/games/fceux`)
- プロジェクト雛形: `Makefile`（make / run / clean）、`nes.cfg`（ld65設定）、`src/main.s`

### ✅ 2. Hello World（背景1色塗り）
- iNESヘッダ / リセット処理（vblank2回待ち・RAMクリア）/ パレット書き込み / NMI の基本骨格
- 青い背景が出ることを確認

### ✅ 3. コントローラ入力
- `read_controller`: `$4016` に 1→0 でラッチ → 8回読み出し `pad1` に格納（A B Sel St ↑ ↓ ← →）
- デモ: 押したボタンで背景色 `$3F00` が変化
- 動作確認OK

### ✅ 4. スプライト表示＋移動
- CHRタイル1にキャラ絵（8x8の赤い丸、目2px透明）を定義
- OAMバッファ `$0200` を起動時 `$FF` で初期化（全スプライト画面外）
- 毎フレームNMIで OAM DMA（`$4014`←`$02`）転送、スクロール0リセット
- `move_player`: 十字キーで `player_x/player_y` を ±2px、画面端でクランプ
- PPUMASK にスプライト表示ON（`%00011110`）
- 動作確認OK（十字キーでキャラが動く）

### ✅ 5. 背景描画（ステージ）
- CHRタイル2にレンガ地面（本体=index1 茶 / 目地=index3 白、上下で継ぎ目をずらす）
- `draw_background`: ネームテーブル `$2000` を 30行×32列で塗る。上27行=空(タイル0)、下3行=地面(タイル2)
- 属性テーブル `$23C0` 64byte は全て BGパレット0
- BGパレット0 を実用色に（`$3F00`=空の青 / 茶 / 明茶 / 目地の白）
- 描画OFF中（PPUMASK設定前）に1回だけ呼ぶ（960+64byte 転送）
- 動作確認OK（青空＋レンガ地面の上をキャラが動く）

### ✅ 6. 重力・ジャンプ・当たり判定
- Y座標を 8.8 固定小数で保持（`player_y`=整数部, `py_sub`=小数部, `vy_hi/vy_lo`=速度）
- 上下の十字キー移動を廃止 → **重力で落下 / A ボタンでジャンプ**
- `GRAVITY`=40(1/256 px/f^2), `JUMP_VEL`=-4.0px/f, 床 `FLOOR_Y`=208（地面 row27=y216）
- ジャンプは A の押した瞬間 & 接地時のみ（`pad1_prev` でエッジ検出、連続ジャンプ防止）
- `FLOOR_Y` で着地スナップ＆停止、`on_ground` フラグ管理
- 起動時は空中スタート → 重力で着地。左右移動は従来通り
- 動作確認OK（落下→着地、左右移動、ジャンプの放物線）
- ※物理パラメータの細かい調整はゲーム像が固まってから

### ✅ 7. キャラのアニメ（歩行＋向き反転）
- キャラを非対称デザインに作り直し（右側に目 / body=index1赤・目=index3白）
- 歩行2フレーム: タイル1(A) ↔ タイル3(B) を 8フレーム周期で交互（`ANIM_MASK`）
- 移動中(`moving`)のみアニメ、停止中はフレームA固定・カウンタリセット
- 進行方向で OAM 属性 bit6(水平反転)を切替（右=`$00` / 左=`$40`、`facing_attr`）
- `animate` が毎フレーム OAM+1(タイル)・OAM+2(属性) をセット
- 動作確認OK（左右で向き反転、移動中だけ足パタパタ）

### ✅ 8. 足場の配置＋乗れる当たり判定
- 足場テーブル `plat_top/x0/x1`（ピクセル座標1本）を描画と当たり判定で共用
- 右上へ階段状に3枚（row22/18/14・幅4/5/5タイル、レンガ=タイル2を流用）
- `draw_platforms`: addr=`$2000 + top*4 + x0/8` に幅分のタイルを上書き
- 当たり判定: `prev_y`(更新前Y)との交差で着地（高速落下でもすり抜けない）
- 上昇中(`vy<0`)はスキップ＝**一方通行**（下から飛び抜けて上に乗れる）
- 足場に乗れなければ既存の地面(`FLOOR_Y`)へフォールバック
- 動作確認OK（階段を1→2→3枚目と登れる、端で落下→地面で停止）

### ✅ 9. 横スクロール（2画面ワールド）
- iNESヘッダを**垂直ミラー**(flags6 bit0=1)に変更＝ネームテーブルを左右配置
- 起動時に**両ネームテーブル($2000/$2400)へ512px幅ステージを描画**（カラムフィード無し・VRAMに丸ごと収める）
- キャラXを**16bitワールド座標**(`px_lo/px_hi`)化、左右移動・クランプも16bit
- `update_camera`: `camera = px - 120` を [0,256] にクランプ、`screen_x = px - camera`
- 毎フレームNMIで `PPUCTRL`のネームテーブルbit(`camera_hi`)＋`PPUSCROLL`(`camera_lo`)でスクロール
- 足場テーブルを16bit座標化（`plat_x0_lo/hi`等）、描画は画面別アドレス・当たり判定は16bit比較
- 足場6枚（左画面3＋右画面3）
- 動作確認OK（右進でスクロール、端でカメラクランプ、右画面の足場にも乗れる）

### ✅ 10. コイン収集＋得点（ゲーム性の第一歩） ← イマココ
- **コイン8枚をスプライトで表示**（OAMスロット1-8）。プレイヤー=スロット0、得点HUD=スロット9
- CHR追加: タイル4=コイン（金の丸＋中央の白い光沢）、タイル5-14=数字'0'-'9'
- スプライトパレット: 0=キャラ赤 / 1=コイン金($28) / 2=得点白($30)
- コイン座標は16bitワールド座標（`coin_x_lo/hi`, `coin_y`、RODATAの定数）。取得状態のみ`coin_active`(RAM 8byte)で管理
- 配置=順路: 左地面→左の足場3枚→右の足場3枚→右地面（各足場の少し上に浮かせる）
- **順番制**: `next_coin`(次に取る番号)を持ち、その番号のコインに重なった時だけ得点+1して次へ。順番を破っても取れない
- 当たり判定`update_coins`: next_coinのコインだけ対象。`|px-cx|<=7`(16bit) かつ `|player_y-cy|<=7`。差+7が[0,14]かで判定
- **狙いのコインを点滅**(`BLINK_MASK`=8フレーム周期)させて目印に
- 描画`draw_coins`: 各コインを `画面内X = cx - camera` で表示、画面外/取得済はY=$FFで隠す
- 得点HUDは**スクロール非依存**（画面左上に固定スプライトで1桁、タイル='0'+score）
- ビルドOK（警告なし、40976byte）。手元の `make run` で目視は問題なさそう
  - ※自動での動作検証は別途検討（この環境はWayland+FCEUXでGUI確認/Lua操作が不安定。nes-pyヘッドレス検証は導入済みだがボタンbit対応合わせが未完）
  - 現方針: 安定検証より「アイデアを形にする」ことを優先

#### 10b. 得点をコンボ加点方式に変更（方法A）
- 1枚1点 → **連続取得でボーナスが増える**方式に。`combo_rem`(初期=COIN_MAX=64)を取得ごとに半減し `bonus = COIN_MAX - combo_rem` を加算
  - ボーナス推移: 32 → 48 → 56 → 60 → 62 → 63 …（8枚連続で計447点）
- ⚠️**NESのCPU(2A03)はBCD無効**。得点は16bit2進(`score_lo/hi`)で加算し、表示時に`bin2dec`で10進変換（1000/100/10で順次引き算）
- HUDを**4桁**に拡張（`score_dec[4]`、OAMスロット9-12）
- コンボのリセット契機: **今はリセット無し**（取るほど増える一方）で進める方針に決定。
  - 将来リセットを入れる時は **即リセット（combo_rem を COIN_MAX に戻す＝次のボーナスは32から）** とする。トリガ候補=タイマー切れ/着地/敵接触は後日
- ✅**実機FCEUXで動作確認OK**: 8枚全取得で **447点**（=理論値32+48+56+60+62+63+63+63 ぴったり）

#### 10c. どのコインも取得可＋順番外はコンボリセット
- 旧: 点滅中(next_coin)の1枚しか取れなかった → **どのコインも触れたら取れる**ように変更
- `update_coins` を全コイン走査に。当たり判定は `coin_overlaps`(X=コイン番号→C)に切り出し
- 取得時、`cpx next_coin` で順番判定:
  - 順番どおり(= 最小未取得) → コンボ継続（32→48→…）
  - 順番外 → `combo_rem = COIN_MAX` にリセット → そのボーナスは **32 に戻る**（総スコアは加算継続）
- 取得後に `next_coin = 最小の未取得インデックス` を再計算（点滅対象＆順番判定に共用）
- nes-py検証OK: 順番外取得=+32(リセット) / 順番どおり=コンボ継続 を確認

#### トラブルメモ: ジャンプで画面が真っ黒に固まる → GLが原因
- 症状: 起動はするが、ジャンプ（最初の入力イベント）でFCEUXが真っ黒のままフリーズ
- 切り分け: nes-pyでは同ジャンプが正常描画＝**ROMは無実**。原因は**このマシンのMesaハードウェアGLがクラッシュ**していた
- 対策: **`LIBGL_ALWAYS_SOFTWARE=1`**（ソフトウェア描画）で解消。Makefileの`run`に組み込み済み（`make run`でOK）
- 補足: 以前のFCEUX不安定（buffer overflow連発）は、検証で`--loadlua`したパスが`~/.fceux/fceux.cfg`の`SDL.LuaScript`に永続保存され毎回自動ロード→FCEUXのLuaバグでcrashしていたのが原因。空に戻して解決済み

### ✅ 11. 障害物（トゲ）＋死亡リスタート ← イマココ
- 地面に**固定のトゲ**を4本配置（`spike_*`テーブル / ワールド座標 / y=FLOOR_Y=208）。CHRタイル15＝上向き三角、スプライトパレット3＝灰
- **触れたら即死**（踏みつけ無し）。`check_spikes`がコインと同じ重なり判定（|px-sx|<=7 かつ |player_y-sy|<=7）
- 死亡で**最初から完全リスタート**: `init_game_state`（reset と共用に切り出し）でプレイヤー初期位置・スコア0・コンボMAX・コイン全復活。背景/パレットは不変なので触らず描画ON中に呼んでOK
- `draw_spikes`でトゲ描画（OAMスロット13-16、画面外はY=$FFで隠す）
- NMI順を整理: move→update_coins→check_spikes→update_camera→OAM（リスタート後の位置を同フレームに反映）
- nes-py検証OK: 接触で px→120・score→0・コイン残→8（全復活）/ 空中(y138)ならトゲ上を安全に通過
- 設計判断: 敵タイプは「まずトゲ(固定)→次に動く敵」、死=完全リスタート、踏みつけ無し（即死のみ）
- **次**: 動く敵キャラ（床/足場を左右に往復、端で反転、触れたら同様に即死）

---

## ファイル構成
```
~/code/nes/
├── Makefile          # make / make run / make clean
├── nes.cfg           # ld65設定 (NROM-256, mapper0)
├── src/main.s        # メインソース (現在: 背景ステージ + スプライト移動)
├── README.md
├── work_log.md       # このファイル
├── hardware_issue.md # HW調査メモ (gitignore済・別件)
└── game.nes          # ビルド成果物 (gitignore済)
```

## ビルド & 実行
```bash
make        # game.nes をビルド
make run    # FCEUX で起動
make clean
```

---

## 次の候補（未着手）
1. **複数スプライト** — 8x8を4枚で16x16の大きいキャラ
2. **ゲーム性の拡張** — 全コイン取得でクリア演出 / 敵 / ゴール / タイマー（コイン収集は §10 で導入済み）
3. **サウンド** — APU で効果音（コイン取得音・ジャンプ音など）/ BGM
4. **ステージ拡張** — カラムフィード方式で2画面より広いステージへ
5. **得点の多桁化** — 1コイン=10点等にして2桁以上のHUDへ（現状は0-8の1桁）

> 入力エッジ検出は §6 のジャンプで導入済み（`pad1_prev`）。
> 歩行アニメ・向き反転は §7、足場の配置と当たり判定は §8、横スクロールは §9 で導入済み。
> ※現状ワールドは2画面(512px)固定。これ以上広げるにはカラムフィード（スクロール中に列を描き足す）が要る。

## メモ / 注意
- 実機キー割り当て（矢印/A=F/B=D等）はFCEUX側設定で、NESコードとは独立。
- RAM不安定問題が未解決のため、節目ごとに git commit してローカルに溜めない方針。
